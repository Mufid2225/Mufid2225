name: Generate Bomberman Contribution

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Generate Bomberman SVG
        uses: abozanona/pacman-contribution-graph@main
        with:
          github_user_name: ${{ github.repository_owner }}
          games: 'bomberman'

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Fix SVG (remove Player 2, keep Player 1 alive)
        run: |
          python scripts/fix_bomberman.py dist/bomberman-contribution-graph.svg
          python scripts/fix_bomberman.py dist/bomberman-contribution-graph-dark.svg

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          mkdir -p assets/contribution
          cp dist/bomberman-contribution-graph.svg assets/contribution/
          cp dist/bomberman-contribution-graph-dark.svg assets/contribution/
          git add assets/contribution/
          git diff --cached --quiet || git commit -m "chore: regenerate bomberman contribution"
          git push